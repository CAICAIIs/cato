# Cato
This is now a simple demo for part of cato project, just `protoc-gen-*` plugin
now you can test by
```shell
make install
```
and then cd into a proto project, add `--cato_out=./` in your proto compile command